ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ ru.dev.miv.ApplicationKt.module ]
    }
}

db {
    username = "postgres"
    username = ${?DB_USERNAME}
    password = "postgres"
    password =  ${?DB_PASSWORD}
    url = "jdbc:postgresql://db:5432/ktorjournal?user=postgres"
    url = ${?DB_URL}
}
